<div class="navigation-bar">
    <div class="col-md-8 mx-auto">
        <nav class="navbar navbar-expand-lg">
            <a class="navbar-brand" href="{{ path('homepage') }}">
                <img src="images/logo.svg" alt="" class="img-fluid" width="200px" height="auto" />
            </a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarsExample09"
                aria-controls="navbarsExample09" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarsExample09">
                <ul class="navbar-nav mr-auto">
                    <li class="nav-item">
                        <a class="nav-link hvr-underline-reveal nav-link-home px-3" href="{{ path('homepage') }}">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link hvr-underline-reveal nav-link-timeline px-3" href="{{ path('timelines') }}">TimeLines</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link hvr-underline-reveal nav-link-timeline px-3" href="{{ path('test') }}">test</a>
                    </li>
                </ul>
                <form class="ml-5 mr-3 my-md-0 input-group">
                    <div class="input-group-prepend ml-5">
                        <button type="submit" class="input-group-text" id="basic-addon1"><i class="fa fa-search"
                                aria-hidden="true"></i></button>
                    </div>
                    <input type="text" class="form-control" placeholder="Recherche" aria-label="Recherche"
                        aria-describedby="basic-addon1">

                </form>

                <ul class="navbar-nav">
                    {% if app.user %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle nav-link-compte" href="#" id="dropdown09" data-toggle="dropdown"
                            aria-haspopup="true" aria-expanded="false">
                            {{ app.user.email }}
                        </a>
                        <div class="dropdown-menu p-0" aria-labelledby="dropdown09">
                            <a class="dropdown-item nav-link nav-liens" href="#">Mon Compte</a>
                            <div class="dropdown-divider m-0"></div>
                            <a class="dropdown-item nav-link nav-liens" href="{{path('timeline-new')}}">Créer une
                                timeline</a>
                            <div class="dropdown-divider m-0"></div>
                            <a class="dropdown-item nav-link nav-liens" href="{{path('app_logout')}}">
                                Déconnexion
                            </a>
                        </div>
                    </li>
                    {% else %}
                    <li class="nav-item">
                        <a href="{{ path('app_register') }}" class="nav-link">
                            Inscription
                        </a>
                    </li>
                    <li class="nav-item">
                        <a href="{{ path('app_login') }}" class="nav-link">Connexion</a>
                    </li>
                    {% endif %}
                </ul>
            </div>
        </nav>
    </div>
</div>